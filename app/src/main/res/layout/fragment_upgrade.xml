<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="layout.UpgradeFragment">


    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center">

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="top"
            android:layout_weight="1">

            <TextView
                android:id="@+id/tv_upgrade"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#000000"
                android:layout_gravity="center_horizontal|top"
                android:text="This is a really janky firmware updater.\nDon't use it until the threading is fixed and\na progress bar added.\n\nYOU'VE BEEN WARNED!"/>
        </ScrollView>

        <!--Invisible to start, but we'll show it during the upgrade.-->
        <ProgressBar
            style="@android:style/Widget.ProgressBar.Horizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/pbar_upgrade"
            android:visibility="invisible"
            android:layout_gravity="center" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="FLASH UPGRADE"
            android:id="@+id/but_upgrade"
            android:layout_gravity="center_horizontal|bottom" />
    </LinearLayout>
</FrameLayout>
